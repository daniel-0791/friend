<resources>
     <!-- 公用的Scrollbar thumb的背景 -->
    <style name="photo_ScrollbarStyle">
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbarThumbVertical">@drawable/photo_bg_scrollbar_thumb</item>
        <item name="android:fadingEdge">none</item>
        <item name="android:fadingEdgeLength">0px</item>
    </style>
      <!-- chechbox的样式 -->
    <style name="photo_CheckBox" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/photo_btn_checkbox_selector</item>
        <item name="android:gravity">center_vertical</item>
    </style>
</resources>
